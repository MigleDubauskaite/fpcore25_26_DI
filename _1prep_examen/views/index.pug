doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Practica para examen 1

        style.
            body { font-family: sans-serif; margin: 20px; background-color: #E5E9C5 }
            .iconos { margin-top: 30px; }
    body
        h1 Examen práctica - Componentes visuales

        //- Contenedor donde se insertarán los botones dinámicamente
        div(id="contenedor")

        //- Iconos que mostrarán tooltips al pasar el ratón
        div(class='iconos')
            i(class="icono", data-titulo="Guardar 💾") 💾
            i(class="icono", data-titulo="Eliminar ❌") ❌

        //- Cargar el script con los componentes
        script(src="/js/componentes.js")
